name: 'CI: Monorepo Build'

on:
  pull_request:
    branches: [ main, master, develop ]
  push:
    branches:
      - main
      - master
      - develop
      - 'feature/**'
      - 'hotfix/**'
      - 'release/**'

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Monorepo build (Turbo, Java 25)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Install pnpm
        uses: pnpm/action-setup@v5
        with:
            version: 10.18.3

      # Node + pnpm (Turborepo/Angular/Astro)
      - uses: actions/setup-node@v5
        with:
          node-version: 24           # pnpm 10 requiere Node >=18
          cache: 'pnpm'
          cache-dependency-path: '**/pnpm-lock.yaml'

      # JDK 25 para el backend
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
          cache: 'maven'

      - name: Install deps
        run: pnpm install --frozen-lockfile

      # Cache local de Turborepo (acelera builds repetidos)
      - name: Cache Turborepo
        uses: actions/cache@v5
        with:
          path: .turbo
          key: turbo-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            turbo-${{ runner.os }}-

      - name: Build all packages with Turbo
        run: pnpm turbo run build --color

      # (Opcional) Lint/Test si ya están listos
      # - run: pnpm turbo run lint --color
      # - run: pnpm turbo run test --color

      # Artefactos descargables desde la ejecución (útil para validación manual)
      - name: Upload backend JAR
        if: ${{ always() }}
        uses: actions/upload-artifact@v5
        with:
          name: backend-jar
          path: apps/fichaje-be/target/*.jar
          if-no-files-found: ignore

      - name: Upload frontend dist
        if: ${{ always() }}
        uses: actions/upload-artifact@v5
        with:
          name: frontend-dist
          path: apps/fichaje-fe/dist/**
          if-no-files-found: ignore

      - name: Upload website dist
        if: ${{ always() }}
        uses: actions/upload-artifact@v5
        with:
          name: website-dist
          path: |
            apps/fichaje-website/dist/**
            apps/fichaje-website/build/**
          if-no-files-found: ignore